(executables
 ((names (main))
  (libraries (compiler-libs.common ppx_jane ppx_driver))
  (link_flags (-linkall))
  (preprocess no_preprocessing)))

(provides (ppx-jane (file main.exe)))

(install
 ((section bin)
  (files ((main.exe as ppx-jane)))))
